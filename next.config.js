/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'oaidalleapiprodscus.blob.core.windows.net',
			'via.placeholder.com',
		],
	},
}

module.exports = nextConfig
